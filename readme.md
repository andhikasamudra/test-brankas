## how to run

first setup the .env

```
cp .env.example .env 
```

set the auth token as u desired

```
AUTH_TOKEN=123qwe  // this is the default token that seeded on the form
```

and then run 

```
go run main.go
```